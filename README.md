# Python tool template repository

## Installation 
```bash
# regular editable install
pip install -e .

# development tools install
pip install -e .[dev]
```

Edit the README.md and pyproject.toml upon cloning.


The empty py.typed files communicates that the package is typed for downstream type checkers that use the package
